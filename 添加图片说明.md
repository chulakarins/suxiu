# 如何添加苏绣图案到项目

## 步骤：

1. **在Xcode中打开项目**
   - 打开 `JinxiuZhizao.xcodeproj`

2. **找到Assets.xcassets**
   - 在左侧项目导航器中，找到 `Assets.xcassets` 文件夹
   - 点击打开

3. **添加图片资源**
   - 在Assets中，你会看到已经创建好的 `SuxiuPattern` 图片集
   - 将你的苏绣图案图片（建议命名为 `suxiu-pattern.png`）拖入 `1x` 的位置
   - 或者直接将图片拖到 `SuxiuPattern` 图片集中

4. **图片建议**
   - 推荐尺寸：400x400 到 600x600 像素
   - 格式：PNG（支持透明背景）
   - 背景：建议使用透明背景或白色背景

5. **运行项目**
   - 按 `Cmd + R` 运行项目
   - 图片会自动显示在首页顶部

## 当前实现的功能：

✅ 主页界面布局（根据UI设计图）
✅ 顶部导航栏（菜单、标题、用户头像）
✅ 苏绣装饰图案区域
✅ 主标题文案
✅ 底部输入框和功能按钮
✅ 底部导航栏（首页、市场、AI生成、推荐、我的）

## 颜色方案：

- 背景色：淡雅浅灰蓝 `Color(red: 0.94, green: 0.96, blue: 0.97)`
- 主色调：蓝色 `Color(red: 0.2, green: 0.5, blue: 1.0)`
- 文字：深灰色，透明度85%

## 下一步可以做的：

- 连接AI生成功能到中间的大按钮
- 实现图片选择器
- 实现语音输入
- 添加更多页面（市场、推荐、我的）
