# 锦绣智造：项目体检报告

体检日期：2026-08-02
范围：应用入口、四个主模块、AI Mock 生图、语音/图片输入、本地存储、资源打包、构建测试与 Git 仓库卫生。

## 结论

当前版本适合作为离线比赛演示原型。核心页面和 Mock 生图不依赖网络，工程可以在 iOS 模拟器编译，19 张候选图均随 App 打包。

## 已修复问题

| 级别 | 问题 | 修复结果 |
|---|---|---|
| P0 | 缺少麦克风/语音识别权限说明可能导致系统终止 App | 补齐 Debug/Release 权限说明 |
| P0 | 麦克风权限处于“未决定”时只检查、不发起申请 | 首次使用会真正请求权限，拒绝后显示可理解提示 |
| P0 | 模拟器选择相机可能触发不可用源崩溃 | 打开前检查相机可用性并提示改用相册 |
| P0 | AI 页面嵌套导航且工具栏按钮无响应 | 移除嵌套导航，恢复可靠返回路径，历史/设置入口可用 |
| P0 | SwiftData 主存储与备用存储都失败时存在强制崩溃 | 移除 `try!`，改为可显示的存储不可用错误 |
| P0 | 旧文档和安全测试含外观类似真实密钥的硬编码示例 | 当前版本全部替换为不可用占位符；若旧值曾真实使用，仍应在供应商控制台撤销 |
| P0 | 删除历史时先删文件，数据库失败会留下损坏记录 | 改为先删记录，再清理文件 |
| P1 | 图片保存成功但数据库写入失败会留下孤立文件 | 失败/取消时清理尚未入库的图片 |
| P1 | 社区“交流”按钮无响应 | 增加完全本地的研习交流页和发送交互 |
| P1 | `MarketProduct4` 资源未分配产生构建警告 | 补全图片集描述 |
| P1 | Bundle Identifier 无法用于规范真机版本 | 统一为 `com.chulakarins.suxiu` 及测试标识 |
| P1 | Info.plist 允许任意明文网络请求 | 删除全局放行，仅保留未来本地开发所需的本地网络许可 |
| P1 | 临时抓取目录、断点文件可能进入版本库 | 补充 `.gitignore` 规则 |

## 已验证

- Debug / iOS Simulator 编译成功。
- 资源目录包含 19 张 Mock JPEG 和 `manifest.json`。
- 正常生成随机耗时约 5–8 秒。
- `MockImageGeneratorTests` 共 8 项测试通过，0 失败。
- App 已在 iPhone 17 Pro / iOS 27 模拟器完成启动检查。
- 生成结果写入 Application Support，数据库只保存相对路径。
- 文化和社区使用的开放素材均有独立来源文档。

## 保留但不影响离线演示的事项

- `APIClient.swift` 和 `suxiu-backend/` 是未来真实服务基础，当前主流程不会调用。
- 文化文章、外部视频和馆藏原页需要网络；本地导读与图片不受影响。
- 社区交流只在当前页面内存在，关闭后不会保存或上传。
- 市集价格、工时、工坊、订单和专家点评均为演示数据。
- 真机首次使用语音、相机或相册时仍会出现正常的系统权限弹窗。

## 建议的下一轮工作

1. 增加一条 UI 自动化测试，固定覆盖首页 → AI → 生成 → 历史。
2. 在最终比赛设备上完成签名、权限与无网演练。
3. 决定正式 AI 供应商后，只新增 `ImageGenerating` 适配器，不改现有 UI 和存储层。
4. 上线真实社区或交易前，补账号、审核、隐私、内容许可和支付合规。
