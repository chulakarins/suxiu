# 锦绣智造：动效与交互设计审查及代码库改进计划

**审查日期**：2026-08-08
**适用版本**：当前 `JinxiuZhizao` SwiftUI 代码库，iOS 26+
**审查范围**：当前可达的文化、学习、AI 创作、社区、市集、个人页和生成历史；同时区分遗留/未接入页面
**设计基准**：Apple 平台的清晰层级、直接操控、空间连续性、系统原生转场、克制反馈、Reduce Motion 与 VoiceOver 兼容

> 说明：仓库与本地可用 skill 中没有找到用户指定的 `apple-design`，本次采用 `redesign-existing-projects` 的现状审计方法，并以 Apple 平台交互原则完成 SwiftUI 专项设计。

## 结论先行

当前应用的视觉完成度已经明显高于动效完成度。页面结构、品牌色、图片质量和 iOS 26 Liquid Glass 基础都已具备，但关键任务流仍以“状态直接替换”为主。

- 当前可达业务界面中只有 **6 类显式动画**；其中录音脉冲为无效实现，课程步骤与市场模式切换只完成了局部动画。
- 系统 `NavigationStack` 和 `.sheet` 的原生转场是当前最稳定的部分，应继续保留。
- 主 Tab 只有选中胶囊和图标在移动，页面、标题与滚动上下文直接替换，连续性不足。
- AI 生成是最值得投入动效的核心路径，但现在从提交、等待、阶段变化到结果出现基本都在瞬间切换。
- 所有自定义交互缺少统一按压态和触觉反馈；没有读取 `accessibilityReduceMotion`。
- 有多个“看起来能点、实际无动作”的入口，必须先修交互真实性，再增加动画。

总体判断：**不需要更多装饰动画，需要一套围绕状态、空间和反馈的系统动画。** 动效应像丝线一样建立连续关系，而不是像特效一样覆盖页面。

---

## 1. 整体动画判断

### 1.1 动效目标

整套动效以“针落有反馈、丝线有连续、绣面有层次”为隐喻，但表现必须保持 Apple 平台的克制：

1. **解释变化**：用户应知道内容为什么变化、从哪里来、去了哪里。
2. **确认操作**：点击、选择、收藏、发送、取消都应在 100ms 内产生可见或触觉反馈。
3. **维持上下文**：Tab、步骤和筛选变化不应让页面像重新加载；滚动位置和已选状态应保留。
4. **表达进度**：AI 生成、图片读取、课程完成度应显示真实状态，而不是只放一个通用转圈。
5. **不过度表演**：首屏内容不做循环漂浮，列表不做大规模逐项飞入，普通页面不自定义系统导航转场。
6. **尊重系统设置**：Reduce Motion 开启后移除位移、缩放、旋转和视差，只保留短淡入淡出或直接切换。

### 1.2 是否应该动画的判断树

| 变化类型 | 是否动画 | 处理方式 |
|---|---:|---|
| 触控按下、选中、收藏、发送 | 是 | 80–240ms 微交互 + 对应触觉反馈 |
| Tab、筛选、步骤、模式切换 | 是 | 共享选中体连续移动；内容使用有方向的短转场 |
| 导航 push、sheet、Menu、系统分享 | 使用系统动画 | 不叠加自定义全屏转场 |
| 异步加载和 AI 生成 | 是 | 阶段进度、可取消状态、结果接力出现 |
| 成功、错误、完成课程 | 是 | 一次性符号反馈 + success/error haptic；不循环 |
| 图片解码完成 | 轻量 | 120–180ms 淡入；不能引起布局跳动 |
| 纯装饰、背景、卡片常驻 | 通常否 | 仅在首次重点揭示且不妨碍阅读时使用 |
| 大型列表整体重排 | 谨慎 | 保持稳定 ID；只动画受影响元素，避免整页弹簧 |

### 1.3 动效级别与统一参数

建议在 `Core/DesignSystem/SuxiuMotion.swift` 中建立语义 token，不允许页面自行散写时长：

| Token | 建议参数 | 用途 |
|---|---|---|
| `micro` | `easeOut 0.14s` | 按压、清除、轻量显隐 |
| `selection` | `snappy 0.24s, extraBounce 0.02` | Tab、Chip、分段选择 |
| `content` | `smooth 0.30s` | 步骤、筛选结果、小区域内容替换 |
| `reveal` | `spring 0.38s, bounce 0.08` | 生成结果、成功卡片一次性出现 |
| `compare` | `easeInOut 0.28s` | 修复前后遮罩或图像参数变化 |
| `activityLoop` | `linear 0.9s repeat` | 仅在录音/生成正在进行时使用 |
| `reduced` | `linear/easeOut 0–0.12s` | Reduce Motion 下只做淡入淡出或无动画 |

硬性限制：

- 直接操控反馈最长 240ms；普通内容转场最长 380ms。
- 不对整个长 `ScrollView` 使用弹簧布局动画。
- 不同时使用 `withAnimation` 和子视图 `.animation(value:)` 驱动同一状态。
- 不使用无期限循环装饰；循环动画必须由明确的活动状态控制并在离屏、取消、完成时停止。
- 只动画 `opacity`、`transform`、`trim`、遮罩等低风险属性；避免在动画中同步解码大图。

### 1.4 触觉与声音判断

统一使用 SwiftUI `.sensoryFeedback`，不自行创建到处散落的反馈生成器：

| 事件 | 反馈 |
|---|---|
| Tab、Chip、步骤选择 | `.selection` |
| 收藏、点赞、加入挑战 | `.impact(weight: .light)` |
| AI 生成完成、课程完成、评论发送 | `.success` |
| 生成失败、权限失败 | `.error` |
| 取消生成 | `.warning` 或轻量 impact，避免成功感 |

同一操作只触发一次反馈；系统 sheet、键盘和 Menu 已有平台反馈时不重复添加声音。

---

## 2. 整套动效与交互描述

### 2.1 主导航与首页

- 点击底部 Tab 后，玻璃栏内的选中胶囊沿最短路径滑向目标；图标通过 SF Symbol replace 过渡从线框变为填充，轻微放大到 1.04 后回稳。
- 页面内容按 Tab 顺序进行 8–12pt 的水平短位移 + 淡入；向右侧 Tab 前进时新内容从右侧进入，返回时方向相反。
- 导航标题与页面内容在同一 240–300ms 事务中变化，避免标题先变、内容后变。
- 每个 Tab 保留自己的滚动位置和局部状态；切走再回来不重新从顶部出现。
- 中央 AI 按钮按下缩至 0.96，蓝色玻璃高光收紧；松开后使用系统 push 进入创作页，不自定义全屏飞入。

### 2.2 文化模块

- 首屏不做持续漂浮。卡片只在按下时产生 0.98 缩放与高光变化，松开后由系统 sheet 承接详情。
- “AI 数字修复”使用可拖动的前后对比滑杆作为首选方案，用户直接拖动遮罩比较原始影像与 AI 推测区域；拖动期间不使用弹簧。
- 若保留当前按钮切换，则图像饱和度、对比度和棕色遮罩在 280ms 内过渡，标签使用 symbol/text replace；不再把动画绑定到整个容器。
- 外部资料继续使用系统 Link/Safari，不叠加自定义转场。

### 2.3 学习模块

- 课程卡片使用统一按压态；进入课程继续使用系统 sheet。
- 进度环在学习数据首次载入时从旧值平滑更新到新值，不从 0 反复表演。
- 步骤前进时，当前说明向左淡出，新说明从右侧 12pt 淡入；返回步骤时方向相反。
- 步骤 Chip、进度条、正文和底部 CTA 必须由同一个 `currentStep` 动画事务驱动；“下一步”按钮不能再绕过动画。
- 完成最后一步时触发一次 success haptic，进度到 100% 后再关闭 sheet，留出约 220ms 让用户看到完成反馈。

### 2.4 AI 创作模块（最高优先级）

- 创作参数选择使用共享选中背景或 matched geometry 轻滑到新选项，文字颜色同步过渡；每次有效变化触发 selection haptic。
- 参考图加入时以 0.96 → 1.0 + 淡入出现；删除时先缩小淡出，再收拢横向空间。
- 输入栏的麦克风、发送、停止、取消使用 `.contentTransition(.symbolEffect(.replace))`，避免四个按钮瞬间换形。
- 开始生成后不要让原结果和内容区突然清空：
  - 首次生成显示与结果卡同尺寸的状态容器；
  - 重新生成时保留旧图并降低到 0.72 透明度，叠加进度层；
  - 取消后恢复旧图；
  - 成功后新图与旧图短交叉淡化，避免白屏。
- 状态文案“检查 → 排队 → 生成 → 下载 → 保存”使用数字/文本内容过渡；进度使用可测量的 `Double`，不能只把百分比埋在字符串里。
- 生成完成时，图片以 0.985 → 1.0 + 淡入出现，动作栏延迟 60ms 接力出现，触发一次 success haptic，并自动滚动到结果顶部但不抢夺正在编辑的输入焦点。
- 错误在原状态卡位置内联出现，不让页面高度突变；错误反馈一次后停止，不闪烁。
- 录音状态使用真实脉冲值或 SF Symbol variable color，不用当前无效的 `repeatForever`。

### 2.5 社区模块

- 加入挑战：按钮宽度保持稳定，`plus → checkmark` 使用符号替换，背景轻微变亮，触发轻触觉。
- Feed 筛选：选中胶囊滑动；列表按稳定 ID 做插入/删除转场，不能整页重建或跳回顶部。
- 点赞：心形一次 bounce + 颜色变化 + light impact；数字使用 numeric content transition。
- 评论发送：新评论从底部 8pt 淡入，自动滚动到新评论；发送按钮在空文本时保留布局，只改变可用状态。

### 2.6 市集与定制流程

- 三段模式选择使用单一移动选中背景。
- “匠人现货 / 纹样授权”的网格差异用 180–240ms 淡入淡出，保持容器顶部稳定。
- 切到“AI 定制”时不能让整个长页面参与弹簧高度变化；只替换模式内容区，并对后续时间线做稳定布局。
- 定制步骤与课程步骤采用同一方向转场组件；进度条和图标同步更新，完成时 success haptic 后关闭。

### 2.7 个人页与生成历史

- 个人页中带箭头的“我的订单 / 我的收藏 / 历史记录”必须先变成真实 `Button`/`NavigationLink`，否则移除箭头和按压暗示。
- 作品卡只有在有详情页时才增加按压和放大预览；没有目的地时保持静态图片。
- 历史收藏采用本地乐观更新，只动画被点击的一行和心形，不通过重新加载整个数组造成闪动。
- 缩略图解码完成后做 120ms 淡入；删除继续使用系统 swipe delete，不增加自定义飞出。

---

## 3. 值得修改的地方

### P0：进入动效开发前必须修复

1. **录音脉冲实际上不动**
   `Features/AIGenerate/AIGenerateView.swift:542–558` 将循环动画绑定到 `isRecording`，但 Circle 没有任何依赖该状态的可动画属性，而且视图只在录音时才插入。该实现是 no-op。

2. **缺少 Reduce Motion 分支**
   全仓库没有读取 `accessibilityReduceMotion`。后续一旦加入位移、缩放、重复动画，会直接形成无障碍缺口。

3. **存在虚假可点击入口**
   - `Features/Experience/ExperienceViews.swift:258` 的“档案索引”显示为按钮，但沿用默认空 action。
   - `HomeView.swift:387–431` 的个人页菜单带箭头但不是 Button。
   - AI 更多菜单里的“我的作品”与“历史记录”走相同处理，语义与结果不一致。

4. **主 Tab 的状态连续性不足**
   `HomeView.swift:1125–1139` 通过 `switch` 直接替换不同类型页面，滚动位置和局部状态可能被销毁；动效只能覆盖选中胶囊，不能解决上下文丢失。

5. **设计文档与实现不一致**
   文档声明了魔法按钮旋转、结果出现、输入按钮切换和统一 Animation token，但活跃代码没有实现这些行为。文档里的 sheet 自定义 `.transition` 也不应作为系统 presentation 动画的实现方案。

### P1：核心体验质量

1. AI 生成过程从输入、等待到结果出现全部硬切换，且重新生成会先清空旧结果。
2. `UIImage(contentsOfFile:)` 在 `ResultView` 的 body 中同步解码，可能在结果入场第一帧造成卡顿；应在 ViewModel/异步图片层解码和缓存。
3. 主 Tab 同时用 `withAnimation` 与 `.animation(value:)` 驱动同一状态，动画事务重复且边界不清晰。
4. 课程“点击步骤 Chip”有动画，但底部“下一步”直接更新状态；相同状态有两种体验。
5. 市场模式把整个条件布局放进 spring 事务，容易产生大范围布局弹跳与滚动位移。
6. 所有 custom/plain button 没有统一按压态；收藏、点赞、选择、完成缺少触觉反馈。
7. 社区筛选、点赞、评论插入、历史收藏全部瞬时变化，没有稳定 ID 驱动的局部过渡。

### P2：系统化与维护性

1. 动画参数分散为 0.2、0.25、0.28、0.32、0.45、0.8 等魔法数字。
2. `HomeView.swift` 约 1213 行、`ExperienceViews.swift` 约 1705 行，不利于统一交互组件与逐页回归。
3. 仓库保留 `ContentView`、`ExploreHomeView`、`MarketView`、`RecommendView` 等未接入或遗留体验，形成第二套视觉/交互语言。
4. `AsyncImage` 没有明确 transaction，远程课程封面载入会突然替换占位内容。
5. 现有所谓 UI/Design 测试多数只比较硬编码常量，没有真实点击、转场或 Reduce Motion 验证。
6. UI Test target 没有可执行测试 bundle，当前整套 `xcodebuild test` 会失败，无法作为动效回归门禁。

---

## 4. 已有动画严格审查

### 4.1 当前可达动画

| 位置 | 当前实现 | 严格判断 | 改进 |
|---|---|---|---|
| `HomeView.swift:1183–1207` | Tab 使用 spring、图标 1.08 缩放、matched geometry 胶囊 | 基础有效；`withAnimation` 与 `.animation` 重复；页面本体硬切换 | 保留胶囊，缩放降至 1.04，增加方向内容转场与 selection haptic，移除重复隐式动画 |
| `AIGenerateView.swift:195–219` | 创作参数颜色/背景 spring 变化 | 有效但反馈弱；没有共享选中体、按压态、触觉 | 统一 `selection` token + matched geometry + `.sensoryFeedback(.selection)` |
| `AIGenerateView.swift:542–558` | 红点绑定 repeatForever pulse | **无效动画**；没有 animatable value 改变 | 使用 phase animator/显式 `pulseScale`，录音停止与 Reduce Motion 时停用 |
| `ExperienceViews.swift:626–635` | 修复图 saturation/contrast/overlay 0.45s | 图像参数有效；动画作用域过宽，标签文字仍硬切，时长偏长 | 首选拖动对比；否则将动画限制在图像层，改 0.28s，并补标签 replace |
| `ExperienceViews.swift:1074–1085` | 点击步骤 Chip 时 easeInOut 0.2s | 只动颜色和 ProgressView；说明文字硬换；CTA advance 不走动画 | 建立统一 `setStep(_:direction:)`，进度、Chip、正文、CTA 同事务 |
| `ExperienceViews.swift:1539–1553` | 市场分段 spring 0.32s | 选中状态有效；条件内容与长页面布局也被卷入 spring | 单独动画 selection indicator；内容区使用短 opacity/directional transition |

### 4.2 遗留或不可达动画

| 位置 | 判断 |
|---|---|
| `HomeView.swift:74–91` | `ExploreHomeView` 分类 spring 当前不在实际入口路径内，不应计入活跃体验完成度。 |
| `ContentView.swift` | 设计文档提到的结果 transition 与输入按钮 transition 没有实现，且 App 当前入口不是该页面。 |
| `HomeView.swift:719–954` | `RecommendView` 的 Tab/Chip 等存在静态替换或无交互状态，当前不属于主导航内容。 |

### 4.3 应保留的系统行为

- `NavigationStack` 的 push/pop。
- `.sheet`、`Menu`、系统分享、图片选择器和 Safari 的系统 presentation。
- iOS 26 `.glassEffect(.interactive())` 对底部玻璃栏和 AI 入口提供的材质响应。
- List 的系统 swipe-to-delete。

不要在这些系统转场上再叠加整页 `.transition`；只设计 sheet 内部的状态变化。

---

## 5. 代码库改进计划

### 5.1 目标结构

```text
JinxiuZhizao/
├── Core/
│   └── DesignSystem/
│       ├── SuxiuMotion.swift              # 动画 token + Reduce Motion 解析
│       ├── SuxiuButtonStyles.swift        # 按压、玻璃、主按钮样式
│       ├── SuxiuFeedback.swift            # 语义反馈映射
│       └── SuxiuTransitions.swift         # 方向内容、结果 reveal、symbol replace
├── Features/
│   ├── Navigation/
│   │   └── SuxiuTabContainer.swift        # 保留各 Tab 状态与滚动位置
│   ├── AIGenerate/
│   │   ├── GenerationStatusCard.swift
│   │   └── GeneratedResultView.swift
│   ├── Experience/
│   │   ├── Heritage/
│   │   ├── Learning/
│   │   ├── Community/
│   │   └── Marketplace/
│   └── Profile/
└── JinxiuZhizaoUITests/
    ├── NavigationMotionUITests.swift
    ├── AIGenerationFlowUITests.swift
    └── ReducedMotionUITests.swift
```

### 5.2 分阶段实施

#### Phase 0：基线与交互真实性（0.5–1 天）

- [ ] 标记实际可达页面；删除或明确归档遗留页面，避免两套交互继续分叉。
- [ ] 修复“档案索引”、个人页菜单、“我的作品”等虚假/错误入口。
- [ ] 为核心路径补稳定 accessibility identifier，供 UI 测试使用。
- [ ] 修复空 UI Test target，使测试 bundle 可执行。
- [ ] 录制当前 Tab、AI 生成、课程步骤、社区点赞和定制步骤作为 before 基线。

**完成标准**：界面中所有带 Button 视觉和 chevron 的元素都有真实动作；`xcodebuild test` 不再因 UI Test bundle 缺失失败。

#### Phase 1：动效基础设施（1 天）

- [ ] 新建 `SuxiuMotion` 语义 token，替换页面内魔法数字。
- [ ] 新建统一按压 ButtonStyle；保证最小 44×44pt 命中区。
- [ ] 在统一容器读取 `accessibilityReduceMotion`，为每个 transition 提供 reduced 分支。
- [ ] 增加 selection / impact / success / error 语义反馈封装。
- [ ] 更新 `设计系统文档.md`，删除未生效或不符合系统 presentation 的示例。

**完成标准**：新增动效不再直接散写时长；Reduce Motion 开启后无位移、缩放、旋转和循环脉冲。

#### Phase 2：主导航连续性（1 天）

- [ ] 将当前 `switch` 替换为能保留各 Tab 状态的容器，例如自定义 `TabView(selection:)` + 底部玻璃栏。
- [ ] 让 selection capsule、图标、标题和内容共享一次有边界的动画事务。
- [ ] 增加方向判断，但位移控制在 8–12pt；Reduce Motion 下使用短淡入淡出。
- [ ] Tab 选择增加 `.sensoryFeedback(.selection)`。
- [ ] 验证每个 Tab 的滚动位置在切换后保留。

**完成标准**：快速连续切换 Tab 不重置页面、不发生整页弹跳、不出现标题/内容不同步。

#### Phase 3：AI 生成核心路径（2 天）

- [ ] 将生成阶段从展示字符串升级为可渲染的 presentation state，包含 stage、progress、isCancellable 和 result continuity。
- [ ] 实现输入按钮 symbol replace、参数选择 indicator、参考图插入/删除转场。
- [ ] 用状态卡替代通用 ProgressView；显示真实阶段和百分比。
- [ ] 重新生成时保留旧结果；成功后交叉淡化到新结果。
- [ ] 将图片读取/解码从 View body 移出，避免结果 reveal 首帧卡顿。
- [ ] 修复录音脉冲，并在 Reduce Motion 下改为静态红点 + “录音中”文字。
- [ ] 对完成、失败、取消和反馈加入一次性语义触觉。
- [ ] 使用 `ScrollViewReader` 把结果滚动到可见位置，不打断输入焦点。

**完成标准**：正常、慢速、失败、超时、离线、取消、重新生成 7 条路径均无白屏、无布局跳跃、无重复 haptic。

#### Phase 4：四大模块与个人页（2 天）

- [ ] 文化：实现修复前后滑杆或受限 compare 动画。
- [ ] 学习：统一步骤方向转场、进度与完成反馈。
- [ ] 社区：筛选局部 diff、点赞 bounce、评论插入和自动滚动。
- [ ] 市集：分段 indicator、稳定内容容器、定制步骤转场。
- [ ] 个人页：真实导航动作与统一按压态。
- [ ] 历史：收藏乐观更新、缩略图淡入、避免全列表 reload 闪动。

**完成标准**：相同交互在不同模块共享相同反馈语言；没有页面专属的随机弹簧参数。

#### Phase 5：严格 QA 与性能门禁（1–1.5 天）

- [ ] 真机/模拟器覆盖 60Hz 与 120Hz，使用 Core Animation Hitches 检查核心路径。
- [ ] 验证 Reduce Motion、VoiceOver、加粗文字、提高对比度与最大 Dynamic Type。
- [ ] 验证 iPhone 17、Pro Max、窄屏和 iPad；横向滚动区不得误触纵向页面。
- [ ] UI 测试覆盖 Tab 状态保留、AI 状态转换、步骤方向和 Reduce Motion 分支。
- [ ] 对动画中图片解码、列表重排和主线程工作做 Instruments 复核。

**完成标准**：

- 核心交互在 100ms 内出现反馈。
- 60Hz 帧预算 16.67ms、120Hz 帧预算 8.33ms；核心演示路径无明显 hitch。
- Reduce Motion 下功能和信息完全等价。
- 快速连点、切后台、取消任务、sheet 中途关闭不会留下重复动画或循环任务。
- 所有 UI 测试与现有单元测试通过。

### 5.3 推荐实施顺序

```text
交互真实性
  → 动效 token / Reduce Motion / ButtonStyle
  → 主 Tab 连续性
  → AI 生成关键路径
  → 学习与定制步骤组件
  → 社区和历史局部反馈
  → 文化前后对比
  → 性能与无障碍回归
```

不要先做首屏花哨入场；先完成 Tab、AI 生成和步骤流程，这三处决定用户是否觉得应用“像一个完整的 Apple 平台产品”。

---

## 验证记录

- 2026-08-08 已按本计划完成第一轮源码实施：统一动效 token、按压样式、语义触觉与 Reduce Motion 基础已接入。
- 主 Tab 已改为保留各页面实例与滚动上下文的连续容器；AI 生成支持旧结果保留、结构化阶段进度、取消/失败反馈和新结果接力出现。
- 已完成修复图拖动对比、课程/定制步骤方向转场、社区筛选/点赞/评论、市集分段与历史收藏局部反馈。
- 已补齐原本为空的 `JinxiuZhizaoUITests` target；主 Tab、快速 AI 生成、Reduce Motion 三条 UI 测试分别通过。
- 8 个 `MockImageGeneratorTests` 全部通过；`build` 与 `build-for-testing` 均通过。
- 仍需在真机完成 60/120Hz Instruments、VoiceOver、最大 Dynamic Type 和切后台中断专项验证。
