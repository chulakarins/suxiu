# 苏绣 AI 后端开发部署指南

**版本**: 1.0.0
**日期**: 2026 年 3 月 28 日
**适用平台**: iOS App 上架必备

---

## 目录

1. [上架 iOS App Store 必备步骤](#1-上架-ios-app-store-必备步骤)
2. [后端服务架构方案](#2-后端服务架构方案)
3. [数据库设计](#3-数据库设计)
4. [用户认证系统](#4-用户认证系统)
5. [API 设计](#5-api-设计)
6. [部署方案对比](#6-部署方案对比)
7. [推荐技术栈](#7-推荐技术栈)
8. [安全与合规](#8-安全与合规)
9. [监控与运维](#9-监控与运维)
10. [成本估算](#10-成本估算)
11. [完整实施计划](#11-完整实施计划)

---

## 1. 上架 iOS App Store 必备步骤

### 1.1 开发者账号准备

| 类型 | 费用 | 功能 | 推荐 |
|------|------|------|------|
| 个人账号 | $99/年 | 发布 App Store | 个人开发者 |
| 公司账号 | $99/年 | + 多成员管理 + 自定义权限 | 创业团队 |
| 企业账号 | $299/年 | + 内部分发 (无 App Store) | 大型企业 |

**所需材料**:
- Apple ID
- 有效信用卡 (Visa/MasterCard)
- 公司名称和 D-U-N-S 编号 (公司账号)
- 双因素认证设备

### 1.2 App Store Connect 配置

```
1. 创建 App ID
   - 登录 developer.apple.com
   - Certificates, IDs & Profiles → Identifiers
   - 注册 Bundle ID: com.yourcompany.suxiuai
   - 启用 Capabilities: Push Notifications, Sign in with Apple

2. 创建证书 (Certificates)
   - Development Certificate (开发证书)
   - Distribution Certificate (发布证书)
   - Push Notification Certificate (推送证书)

3. 创建配置文件 (Provisioning Profiles)
   - Development Profile
   - Ad Hoc Profile (测试用)
   - App Store Profile (发布用)

4. App Store Connect 配置
   - 创建新 App
   - 填写元数据 (名称、描述、关键词)
   - 上传应用截图 (6.7", 6.5", 5.5", 12.9")
   - 设置价格和销售范围
```

### 1.3 隐私合规要求

**必须完成的项目**:

| 项目 | 说明 | 截止时间 |
|------|------|----------|
| 隐私政策 (Privacy Policy) | 必须有可访问的 URL | 提交审核前 |
| 用户协议 (Terms of Service) | 建议使用 | 提交审核前 |
| App 隐私标签 | 在 App Store Connect 填写 | 提交审核前 |
| Sign in with Apple | 如有第三方登录，必须提供 | 开发阶段 |
| 数据删除功能 | 用户必须能删除账户和数据 | 开发阶段 |

### 1.4 提审清单

```
□ 应用完成所有功能测试
□ 无崩溃、无严重 Bug
□ 隐私政策 URL 有效
□ App 隐私标签已填写
□ 截图符合尺寸要求
□ 描述准确无误导
□ 内购项目已配置 (如有)
□ 订阅配置完成 (如有)
□ 测试账号已准备 (如需登录)
□ 联系方式有效
```

### 1.5 审核常见拒绝原因

| 代码 | 原因 | 解决方案 |
|------|------|----------|
| 2.1 | 应用完成度不够 | 确保功能完整，无占位内容 |
| 4.3 | 重复应用 | 确保应用有独特价值 |
| 5.1.1 | 隐私问题 | 完善隐私政策，正确收集数据 |
| 3.1.1 | 内购问题 | 数字内容必须用 IAP，不能用第三方支付 |
| 2.5.4 | 后台运行问题 | 确保应用不崩溃 |

---

## 2. 后端服务架构方案

### 2.1 你的 App 需要的后端功能

根据苏绣 AI 的当前实现，后端需要支持：

```
┌─────────────────────────────────────────────────────────┐
│                     客户端 (iOS App)                      │
├─────────────────────────────────────────────────────────┤
│  功能需求:                                               │
│  1. 用户注册/登录/管理                                    │
│  2. AI 图片生成 (当前用阿里云 API)                         │
│  3. 用户作品存储与展示                                    │
│  4. 订单管理 (C2M 定制)                                   │
│  5. 消息通知 (推送)                                        │
│  6. 数据统计与分析                                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     后端服务层                            │
├─────────────────────────────────────────────────────────┤
│  - API 网关 (RESTful / GraphQL)                          │
│  - 用户认证 (JWT / OAuth2)                               │
│  - 业务逻辑处理                                          │
│  - 第三方 API 代理 (隐藏 API Key)                         │
│  - 文件上传处理                                          │
│  - 消息队列 (异步任务)                                    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     数据存储层                            │
├─────────────────────────────────────────────────────────┤
│  - 用户数据 (PostgreSQL / MySQL)                         │
│  - 缓存 (Redis)                                          │
│  - 文件存储 (OSS / S3)                                   │
│  - 日志存储 (ELK / CloudWatch)                           │
└─────────────────────────────────────────────────────────┘
```

### 2.2 为什么你需要后端

**当前问题**:
```swift
// 当前代码 - API Key 硬编码在客户端
class AIImageService {
    private let apiKey = "sk-d5f6f3edc558444baff5b26af58536f8"  // ⚠️ 不安全!
}
```

**风险**:
1. API Key 可被反编译获取
2. 无法控制 API 调用配额
3. 无法追踪用户使用情况
4. 无法实现计费功能

**解决方案**:
```swift
// 客户端只发送请求，不接触 API Key
func generateImage(prompt: String) async throws {
    let response = try await apiClient.request("/api/generate", method: "POST", body: [
        "prompt": prompt
    ])
    return response.imageURL
}

// 后端安全地调用第三方 API
POST /api/generate
{
    "prompt": "一只猫咪"
}
→ 后端调用阿里云 API →
→ 返回 imageURL 给客户端
```

---

## 3. 数据库设计

### 3.1 核心数据表

```sql
-- 用户表
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email VARCHAR(255) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    nickname VARCHAR(50),
    avatar_url VARCHAR(500),
    phone VARCHAR(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);

-- 作品表
CREATE TABLE artworks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    prompt TEXT NOT NULL,
    image_url VARCHAR(500) NOT NULL,
    thumbnail_url VARCHAR(500),
    status VARCHAR(20) DEFAULT 'generated', -- generated, ordered, completed
    like_count INT DEFAULT 0,
    view_count INT DEFAULT 0,
    is_public BOOLEAN DEFAULT false,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 订单表 (C2M 定制)
CREATE TABLE orders (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    artwork_id UUID REFERENCES artworks(id),
    product_type VARCHAR(50), -- scarf, pillow, wall_art
    size VARCHAR(20),
    price DECIMAL(10, 2),
    status VARCHAR(20) DEFAULT 'pending', -- pending, processing, shipped, completed
    shipping_address JSONB,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 点赞表
CREATE TABLE likes (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    artwork_id UUID REFERENCES artworks(id),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(user_id, artwork_id)
);

-- 使用记录 (用于配额控制)
CREATE TABLE usage_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id UUID REFERENCES users(id),
    action VARCHAR(50), -- generate_image, order_product
    api_request_id VARCHAR(100),
    cost DECIMAL(10, 4),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 3.2 索引优化

```sql
-- 常用查询索引
CREATE INDEX idx_artworks_user_id ON artworks(user_id);
CREATE INDEX idx_artworks_created_at ON artworks(created_at DESC);
CREATE INDEX idx_artworks_public ON artworks(is_public) WHERE is_public = true;
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_users_email ON users(email);
```

---

## 4. 用户认证系统

### 4.1 认证方案选择

| 方案 | 优点 | 缺点 | 推荐场景 |
|------|------|------|----------|
| 邮箱密码 | 简单直接 | 用户体验一般 | 所有应用必备 |
| 手机号验证码 | 中国用户友好 | 需要短信服务 | 中国市场 |
| Sign in with Apple | iOS 最佳体验 | 仅限 Apple 设备 | iOS 应用必须支持 |
| 微信登录 | 中国用户友好 | 需要额外集成 | 中国市场 |
| Google/Facebook | 海外用户 | 国内不可用 | 出海应用 |

### 4.2 JWT Token 实现

```swift
// 客户端存储
struct AuthToken: Codable {
    let accessToken: String
    let refreshToken: String
    let expiresIn: Int
    let tokenType: String // "Bearer"
}

// Keychain 存储
class AuthManager {
    static let shared = AuthManager()

    func save(token: AuthToken) {
        // 存入 Keychain
    }

    func getToken() -> AuthToken? {
        // 从 Keychain 读取
    }

    func refreshTokenIfNeeded() async throws {
        // Token 过期自动刷新
    }
}
```

### 4.3 Sign in with Apple 集成

```swift
import AuthenticationServices

class AppleSignInManager: NSObject, ASAuthorizationControllerDelegate {
    func signIn() {
        let appleIDProvider = ASAuthorizationAppleIDProvider()
        let request = appleIDProvider.createRequest()
        request.requestedScopes = [.fullName, .email]

        let controller = ASAuthorizationController(authorizationRequests: [request])
        controller.delegate = self
        controller.performRequests()
    }

    func authorizationController(controller: ASAuthorizationController,
                                 didCompleteWithAuthorization authorization: ASAuthorization) {
        if let credential = authorization.credential as? ASAuthorizationAppleIDCredential {
            let appleID = credential.user
            let email = credential.email
            // 发送到后端验证
        }
    }
}
```

---

## 5. API 设计

### 5.1 RESTful API 规范

```
基础 URL: https://api.suxiuai.com/v1

认证方式：Bearer Token
请求格式：JSON
响应格式：JSON
```

### 5.2 核心 API 端点

```yaml
# 用户认证
POST /auth/register          # 注册
POST /auth/login             # 登录
POST /auth/logout            # 登出
POST /auth/refresh           # 刷新 Token
POST /auth/apple             # Apple 登录
POST /auth/password/reset    # 重置密码

# 用户信息
GET  /user/profile           # 获取个人信息
PUT  /user/profile           # 更新个人信息
DELETE /user/account         # 删除账户

# 作品管理
GET  /artworks               # 获取作品列表
GET  /artworks/:id           # 获取作品详情
POST /artworks/generate      # 生成新作品
PUT  /artworks/:id           # 更新作品
DELETE /artworks/:id         # 删除作品
POST /artworks/:id/like      # 点赞
DELETE /artworks/:id/like    # 取消点赞

# 订单管理
GET  /orders                 # 获取订单列表
GET  /orders/:id             # 获取订单详情
POST /orders                 # 创建订单
PUT  /orders/:id/status      # 更新订单状态

# 管理后台
GET  /admin/users            # 用户管理
GET  /admin/orders           # 订单管理
GET  /admin/statistics       # 统计数据
```

### 5.3 统一响应格式

```json
// 成功响应
{
    "code": 0,
    "message": "success",
    "data": {
        "id": "uuid",
        "name": "value"
    }
}

// 错误响应
{
    "code": 1001,
    "message": "用户未登录",
    "data": null
}
```

---

## 6. 部署方案对比

### 6.1 方案 A: 云服务厂商 PaaS (推荐初创)

**代表产品**:
- 国内：阿里云·云效、腾讯云·云开发、华为云·应用托管
- 国际：Vercel、Railway、Render、Fly.io

**优点**:
- ✅ 零运维，自动扩缩容
- ✅ 内置 CI/CD
- ✅ 按使用付费，成本低
- ✅ 自带 SSL、域名管理

**缺点**:
- ❌ 定制性有限
- ❌ 厂商锁定风险
- ❌ 高并发时成本上升

**成本估算**:
```
- 免费额度：通常够用 (500-1000 小时/月)
- 基础套餐：$10-50/月
- 数据库：$10-30/月
- 存储：$1-5/月
总计：$20-100/月 (初期)
```

### 6.2 方案 B: 云服务器 + 容器化

**代表产品**:
- 国内：阿里云 ECS、腾讯云 CVM
- 国际：AWS EC2、DigitalOcean、Linode

**优点**:
- ✅ 完全控制权
- ✅ 可运行任意服务
- ✅ 成本可控

**缺点**:
- ❌ 需要运维知识
- ❌ 需要自己配置安全
- ❌ 需要自己处理备份

**成本估算**:
```
- 云服务器 (2 核 4G): $20-40/月
- 数据库 RDS: $15-30/月
- OSS 存储：$5-10/月
- CDN: $5-20/月
- 域名：$10-15/年
总计：$50-120/月
```

### 6.3 方案 C: Serverless

**代表产品**:
- 阿里云函数计算、腾讯云云函数
- AWS Lambda、Cloudflare Workers

**优点**:
- ✅ 按调用付费，极低成本
- ✅ 自动扩缩容
- ✅ 零运维

**缺点**:
- ❌ 冷启动延迟
- ❌ 执行时间限制
- ❌ 不适合长连接

**成本估算**:
```
- 函数调用：$0.0000166667/GB-秒
- API 网关：$3.50/百万请求
- 初期可能免费
总计：$5-50/月 (看用量)
```

### 6.4 方案对比总结

| 维度 | PaaS | 云服务器 | Serverless |
|------|------|----------|------------|
| 上手难度 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐ |
| 运维成本 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐⭐⭐⭐ |
| 灵活性 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 初期成本 | ⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 扩展性 | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**推荐**: 初创阶段选择 **PaaS 方案** (Vercel + Supabase 组合)

---

## 7. 推荐技术栈

### 7.1 最佳组合 (2026 年)

```
┌─────────────────────────────────────────────────────────┐
│                     前端 (iOS)                           │
│  SwiftUI + iOS 26 SDK                                    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     后端 API                             │
│  方案 1: Node.js + Express/Nest.js                       │
│  方案 2: Go + Gin/Echo                                   │
│  方案 3: Python + FastAPI                                │
│  方案 4: 云开发 (腾讯云云开发/阿里云云效)                  │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     数据库                               │
│  PostgreSQL (Supabase / Neon / Railway)                │
│  或 MySQL (阿里云 RDS / 腾讯云 CDB)                        │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                     对象存储                             │
│  阿里云 OSS / 腾讯云 COS / AWS S3                        │
└─────────────────────────────────────────────────────────┘
```

### 7.2 具体推荐 (按场景)

#### 场景 1: 个人开发者，快速上线

```yaml
前端：SwiftUI (已有)
后端：腾讯云云开发 (CloudBase)
  - 免服务器运维
  - 自带数据库
  - 自带云函数
  - 自带存储
  - 免费额度够用
成本：$0-30/月
上线时间：1-2 周
```

#### 场景 2: 创业团队，需要扩展性

```yaml
前端：SwiftUI (已有)
后端：Node.js + Nest.js
部署：Vercel / Railway
数据库：Supabase (PostgreSQL)
存储：阿里云 OSS / AWS S3
成本：$50-150/月
上线时间：2-4 周
```

#### 场景 3: 企业级，高可用

```yaml
前端：SwiftUI (已有)
后端：Go + Gin
部署：Kubernetes (阿里云 ACK)
数据库：PostgreSQL 主从 + Redis 缓存
存储：阿里云 OSS + CDN
监控：Prometheus + Grafana
成本：$500-2000/月
上线时间：1-3 月
```

### 7.3 后端代码示例 (Node.js + Express)

```javascript
// server.js
const express = require('express');
const cors = require('cors');
const jwt = require('jsonwebtoken');
const { Pool } = require('pg');
const axios = require('axios');

const app = express();
app.use(cors());
app.use(express.json());

// 数据库连接
const pool = new Pool({
  connectionString: process.env.DATABASE_URL
});

// 中间件：验证 JWT
const authMiddleware = async (req, res, next) => {
  const token = req.headers.authorization?.replace('Bearer ', '');
  if (!token) return res.status(401).json({ code: 1001, message: '未登录' });

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET);
    req.userId = decoded.userId;
    next();
  } catch {
    return res.status(401).json({ code: 1001, message: 'Token 无效' });
  }
};

// 生成图片 API (代理阿里云 API)
app.post('/api/artworks/generate', authMiddleware, async (req, res) => {
  const { prompt } = req.body;

  try {
    // 调用阿里云 DashScope API
    const response = await axios.post(
      'https://dashscope.aliyuncs.com/api/v1/services/aigc/text2image/image-synthesis',
      {
        model: 'wan2.5-t2i-preview',
        input: { prompt: prompt + ', ultra realistic suzhou embroidery...' },
        parameters: { size: '1280*1280', n: 1 }
      },
      {
        headers: {
          'Authorization': `Bearer ${process.env.ALIBABA_API_KEY}`,
          'Content-Type': 'application/json'
        }
      }
    );

    // 记录使用日志
    await pool.query(
      'INSERT INTO usage_logs (user_id, action, api_request_id) VALUES ($1, $2, $3)',
      [req.userId, 'generate_image', response.data.output.task_id]
    );

    res.json({ code: 0, data: { taskId: response.data.output.task_id } });
  } catch (error) {
    res.status(500).json({ code: 2001, message: '生成失败', error: error.message });
  }
});

// 获取作品列表
app.get('/api/artworks', authMiddleware, async (req, res) => {
  const { page = 1, limit = 20 } = req.query;
  const result = await pool.query(
    'SELECT * FROM artworks WHERE user_id = $1 ORDER BY created_at DESC LIMIT $2 OFFSET $3',
    [req.userId, limit, (page - 1) * limit]
  );
  res.json({ code: 0, data: result.rows });
});

app.listen(3000, () => {
  console.log('Server running on port 3000');
});
```

---

## 8. 安全与合规

### 8.1 必须实现的安全措施

| 项目 | 说明 | 优先级 |
|------|------|--------|
| HTTPS | 所有 API 必须 HTTPS | 🔴 必须 |
| API Key 保护 | 永远不暴露在客户端 | 🔴 必须 |
| 输入验证 | 防止 SQL 注入、XSS | 🔴 必须 |
| 速率限制 | 防止 API 滥用 | 🔴 必须 |
| 数据加密 | 密码哈希存储 | 🔴 必须 |
| 日志审计 | 记录关键操作 | 🟡 推荐 |

### 8.2 密码存储规范

```javascript
// ❌ 错误：明文存储
const password = "user123";

// ❌ 错误：简单 MD5
const hash = md5(password);

// ✅ 正确：bcrypt
const bcrypt = require('bcrypt');
const saltRounds = 10;
const hash = await bcrypt.hash(password, saltRounds);
const isValid = await bcrypt.compare(password, hash);
```

### 8.3 速率限制实现

```javascript
const rateLimit = require('express-rate-limit');

const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 分钟
  max: 100, // 最多 100 请求
  message: { code: 4029, message: '请求过于频繁' }
});

app.use('/api/', limiter);
```

### 8.4 隐私合规清单

```
□ 隐私政策页面 (可访问的 URL)
□ 用户协议页面
□ App 隐私标签 (App Store Connect)
□ 数据导出功能 (GDPR 要求)
□ 账户删除功能
□ Cookie 同意 (如面向欧盟)
□ 儿童隐私保护 (如有未成年用户)
```

---

## 9. 监控与运维

### 9.1 必须监控的指标

| 指标 | 工具 | 告警阈值 |
|------|------|----------|
| API 响应时间 | Prometheus/NewRelic | > 500ms |
| 错误率 | Sentry | > 1% |
| 服务器 CPU | CloudWatch | > 80% |
| 数据库连接 | 自建/云监控 | > 80% |
| 磁盘空间 | 云监控 | > 85% |

### 9.2 日志规范

```javascript
// 结构化日志
const log = {
  timestamp: new Date().toISOString(),
  level: 'INFO',
  userId: 'uuid',
  action: 'generate_image',
  duration: 1234,
  requestId: 'req-123'
};
```

### 9.3 备份策略

```yaml
数据库:
  自动备份：每天凌晨 3 点
  保留策略：7 天每日 + 4 周每周 + 12 月每月
  异地备份：每周一次

文件存储:
  版本控制：开启
  跨区复制：开启 (重要数据)
```

---

## 10. 成本估算

### 10.1 初期 (0-1000 用户)

| 项目 | 方案 | 月成本 |
|------|------|--------|
| 后端托管 | Vercel 免费档 | $0 |
| 数据库 | Supabase 免费档 | $0 |
| 对象存储 | 阿里云 OSS | $5 |
| 域名 | Namecheap | $1/月 |
| API 调用 | 阿里云 DashScope | $20 |
| **总计** | | **~$26/月** |

### 10.2 成长期 (1000-10000 用户)

| 项目 | 方案 | 月成本 |
|------|------|--------|
| 后端托管 | Railway Pro | $29 |
| 数据库 | Supabase Pro | $25 |
| 对象存储 + CDN | 阿里云 | $20 |
| 域名 | - | $1/月 |
| API 调用 | 阿里云 (量大优惠) | $100 |
| 监控 | Sentry | $26 |
| **总计** | | **~$200/月** |

### 10.3 成熟期 (10000+ 用户)

| 项目 | 方案 | 月成本 |
|------|------|--------|
| 云服务器 | 阿里云 ECS | $100 |
| 数据库 RDS | 阿里云 RDS | $100 |
| Redis | 阿里云 Redis | $50 |
| OSS + CDN | 阿里云 | $100 |
| 负载均衡 | 阿里云 SLB | $30 |
| API 调用 | 阿里云 (企业价) | $500+ |
| 监控 + 日志 | 自建 | $50 |
| **总计** | | **~$1000+/月** |

---

## 11. 完整实施计划

### 11.1 第一阶段：MVP (1-2 周)

```
目标：让 App 可以上架，有基本后端功能

任务:
□ 注册 Apple 开发者账号 ($99/年)
□ 选择云开发平台 (推荐腾讯云云开发)
□ 搭建基础 API (用户认证、作品存储)
□ 集成 Sign in with Apple
□ 迁移 API Key 到后端
□ 编写隐私政策和用户协议
□ 配置 App Store Connect
□ 提交审核
```

### 11.2 第二阶段：功能完善 (2-4 周)

```
目标：完善核心功能，提升用户体验

任务:
□ 实现作品云端存储
□ 实现点赞和分享功能
□ 实现推送通知
□ 添加数据统计后台
□ 优化 API 响应速度
□ 添加错误监控 (Sentry)
```

### 11.3 第三阶段：商业化 (1-2 月)

```
目标：实现 C2M 商业闭环

任务:
□ 对接工厂/供应商 API
□ 实现订单管理系统
□ 集成支付 (App Store IAP 或 微信支付)
□ 实现物流跟踪
□ 客服系统
□ 营销工具 (优惠券、邀请码)
```

### 11.4 第四阶段：规模化 (3 月+)

```
目标：支持大规模用户

任务:
□ 数据库读写分离
□ 添加 Redis 缓存
□ CDN 加速
□ 自动扩缩容
□ A/B 测试平台
□ 用户增长分析
```

---

## 附录 A: 快速开始模板

### A.1 使用 Supabase (推荐)

```bash
# 1. 创建 Supabase 项目
# 访问 supabase.com 注册

# 2. 创建数据表 (在 SQL Editor 执行)
-- 复制第 3 节的 SQL

# 3. 客户端集成
# iOS: 使用 supabase-swift SDK
import Supabase

let supabase = SupabaseClient(
  supabaseURL: URL("https://xxx.supabase.co"),
  supabaseKey: "your-anon-key"
)

# 4. 后端函数 (Edge Functions)
# 在 Supabase 控制台创建
```

### A.2 使用 Vercel 部署

```bash
# 1. 安装 Vercel CLI
npm install -g vercel

# 2. 初始化项目
vercel init

# 3. 部署
vercel

# 4. 配置生产环境
vercel --prod
```

### A.3 iOS 客户端配置

```swift
// NetworkManager.swift
class NetworkManager {
    static let shared = NetworkManager()

    private let baseURL = "https://api.suxiuai.com/v1"
    private let session: URLSession

    init() {
        let config = URLSessionConfiguration.default
        config.timeoutIntervalForRequest = 30
        config.timeoutIntervalForResource = 300
        session = URLSession(configuration: config)
    }

    func request<T: Codable>(
        _ endpoint: String,
        method: String = "GET",
        body: Codable? = nil
    ) async throws -> T {
        var request = URLRequest(url: URL(string: baseURL + endpoint)!)
        request.httpMethod = method
        request.setValue("application/json", forHTTPHeaderField: "Content-Type")

        // 添加 Token
        if let token = AuthManager.shared.getToken()?.accessToken {
            request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
        }

        // 添加 Body
        if let body = body {
            request.httpBody = try JSONEncoder().encode(body)
        }

        let (data, response) = try await session.data(for: request)

        guard let http = response as? HTTPURLResponse, http.statusCode == 200 else {
            throw NetworkError.httpError
        }

        return try JSONDecoder().decode(T.self, from: data)
    }
}
```

---

## 附录 B: 检查清单

### 上架前检查

```
技术检查:
□ 无崩溃
□ 所有按钮有功能
□ 加载状态正确
□ 错误处理完善
□ 网络请求有超时处理
□ 图片有缓存
□ 支持深色模式 (可选)

内容检查:
□ 应用名称无侵权
□ 截图准确反映功能
□ 描述无夸大
□ 关键词相关
□ 隐私政策有效

账户检查:
□ 开发者账号已验证
□ 银行卡已绑定
□ 税务信息已填写
□ 联系信息有效
```

---

**文档结束**

*本指南持续更新，最后更新：2026 年 3 月 28 日*
