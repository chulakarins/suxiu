# 苏绣 AI 设计系统文档

**版本**: 1.0.0
**最后更新**: 2026 年 3 月 28 日
**平台**: iOS 26+

---

## 目录

1. [设计原则](#设计原则)
2. [颜色系统](#颜色系统)
3. [字体排印](#字体排印)
4. [间距系统](#间距系统)
5. [圆角系统](#圆角系统)
6. [阴影系统](#阴影系统)
7. [ Liquid Glass 效果](#liquid-glass-效果)
8. [组件库](#组件库)
9. [图标系统](#图标系统)
10. [动效设计](#动效设计)

---

## 设计原则

### 1.1 核心理念

**传统与现代的融合**
- 传承苏绣工艺的精致美学
- 运用现代 AI 技术和设计语言
- 创造独特的文化科技体验

### 1.2 设计价值观

| 价值观 | 描述 |
|--------|------|
| 雅致 | 精致而不繁琐，体现苏绣的高雅气质 |
| 直观 | 降低学习成本，让用户自然上手 |
| 流畅 | 丝滑的交互动效，如丝绸般顺滑 |
| 温暖 | 友好亲切的视觉感受，避免冷漠科技感 |

### 1.3 品牌关键词

```
苏绣 · 锦绣 · 匠心 · 传承 · 创新
```

---

## 颜色系统

### 2.1 主色系

#### 背景色

| 名称 | 色值 | 用途 |
|------|------|------|
| Background Primary | `#F1F4F9` | 主背景色 |
| Background Secondary | `#E8EDF5` | 次级背景 |
| Background Tertiary | `#DFE6F0` | 第三级背景 |

**Swift 定义**:
```swift
extension Color {
    static let suxiuBackground = Color(red: 0.945, green: 0.957, blue: 0.976)
    static let suxiuBackgroundSecondary = Color(red: 0.910, green: 0.929, blue: 0.961)
    static let suxiuBackgroundTertiary = Color(red: 0.875, green: 0.902, blue: 0.941)
}
```

#### 强调色

| 名称 | 色值 | 用途 |
|------|------|------|
| Accent Blue | `#0062E7` | 主强调色，用于按钮、链接 |
| Accent Blue Dark | `#004FB8` | 强调色深色状态 |
| Accent Blue Light | `#3385FF` | 强调色浅色状态 |

**Swift 定义**:
```swift
extension Color {
    static let suxiuAccent = Color(red: 0.2, green: 0.48, blue: 0.95)
    static let suxiuAccentDark = Color(red: 0.1, green: 0.35, blue: 0.9)
}
```

#### 文字色

| 名称 | 色值 | 用途 | 不透明度 |
|------|------|------|---------|
| Text Primary | `#111827` | 主标题、正文 | 100% |
| Text Secondary | `#6B7280` | 副标题、说明文字 | 100% |
| Text Tertiary | `#9CA3AF` | 占位符、提示文字 | 100% |
| Text Disabled | `#D1D5DB` | 禁用状态文字 | 100% |

**Swift 定义**:
```swift
extension Color {
    static let suxiuTextPrimary = Color(red: 0.067, green: 0.094, blue: 0.153)
    static let suxiuTextSecondary = Color(red: 0.42, green: 0.447, blue: 0.502)
    static let suxiuTextTertiary = Color(red: 0.616, green: 0.639, blue: 0.686)
}
```

### 2.2 功能色

#### 状态色

| 状态 | 色值 | 用途 |
|------|------|------|
| Success | `#10B981` | 成功、完成 |
| Warning | `#F59E0B` | 警告、注意 |
| Error | `#EF4444` | 错误、失败 |
| Info | `#3B82F6` | 信息、提示 |

#### 在线状态

```swift
// 在线指示器绿色
Color(red: 0.18, green: 0.78, blue: 0.35)  // #2EC759
```

### 2.3 渐变色

#### 强调色渐变

```swift
LinearGradient(
    colors: [
        Color(red: 0.2, green: 0.48, blue: 0.95),   // #337AF0
        Color(red: 0.1, green: 0.35, blue: 0.9)     // #1959E6
    ],
    startPoint: .topLeading,
    endPoint: .bottomTrailing
)
```

**用途**: 魔法按钮、高亮元素

---

## 字体排印

### 3.1 字体家族

| 平台 | 中文字体 | 英文字体 |
|------|---------|---------|
| iOS | PingFang SC (苹方) | SF Pro Display |
| 备用 | Heiti SC (黑体) | San Francisco |

### 3.2 字号系统

基于 SF Pro 和 PingFang SC 的字号规范：

| 级别 | 字号 | 行高 | 字重 | 用途 |
|------|------|------|------|------|
| Large Title | 34pt | 41pt | Bold | 页面大标题 |
| Title 1 | 28pt | 34pt | Bold | 模块标题 |
| Title 2 | 22pt | 28pt | Semibold | 卡片标题 |
| Title 3 | 20pt | 25pt | Semibold | 分组标题 |
| Headline | 17pt | 22pt | Semibold | 重要文本 |
| Body | 17pt | 22pt | Regular | 正文内容 |
| Callout | 16pt | 21pt | Regular | 说明文字 |
| Subhead | 15pt | 20pt | Regular | 次要正文 |
| Footnote | 13pt | 18pt | Regular | 注释文字 |
| Caption 1 | 12pt | 16pt | Regular | 图片说明 |
| Caption 2 | 11pt | 13pt | Regular | 标签文字 |

### 3.3 Swift 字体定义

```swift
extension Font {
    // 标题字体
    static let suxiuLargeTitle = Font.system(size: 34, weight: .bold, design: .default)
    static let suxiuTitle1 = Font.system(size: 28, weight: .bold, design: .default)
    static let suxiuTitle2 = Font.system(size: 22, weight: .semibold, design: .default)
    static let suxiuTitle3 = Font.system(size: 20, weight: .semibold, design: .default)

    // 正文字体
    static let suxiuHeadline = Font.system(size: 17, weight: .semibold, design: .default)
    static let suxiuBody = Font.system(size: 17, weight: .regular, design: .default)
    static let suxiuCallout = Font.system(size: 16, weight: .regular, design: .default)
    static let suxiuSubhead = Font.system(size: 15, weight: .regular, design: .default)

    // 辅助字体
    static let suxiuFootnote = Font.system(size: 13, weight: .regular, design: .default)
    static let suxiuCaption1 = Font.system(size: 12, weight: .regular, design: .default)
    static let suxiuCaption2 = Font.system(size: 11, weight: .regular, design: .default)
}

// 使用示例
Text("标题")
    .font(.suxiuTitle1)

Text("正文内容")
    .font(.suxiuBody)
```

### 3.4 字重使用规范

| 字重 | SwiftUI | 使用场景 |
|------|---------|---------|
| Bold | `.bold` | 大标题、重要强调 |
| Semibold | `.semibold` | 小标题、选中状态 |
| Medium | `.medium` | 按钮文字、标签 |
| Regular | `.regular` | 正文内容、说明文字 |

---

## 间距系统

### 4.1 基础间距单位

基础单位：**4pt**

所有间距应为 4 的倍数：4, 8, 12, 16, 20, 24, 32, 40, 48, 64, 80, 96...

### 4.2 标准间距值

| 名称 | 值 | 用途 |
|------|-----|------|
| xs | 4pt | 紧密元素间距 |
| sm | 8pt | 相关元素间距 |
| md | 16pt | 一般元素间距 |
| lg | 24pt | 分组间距 |
| xl | 32pt | 大区块间距 |
| 2xl | 48pt | 页面级间距 |
| 3xl | 64pt | 超大间距 |

### 4.3 Swift 间距定义

```swift
extension CGFloat {
    // 标准间距
    static let spacingXS: CGFloat = 4
    static let spacingSM: CGFloat = 8
    static let spacingMD: CGFloat = 16
    static let spacingLG: CGFloat = 24
    static let spacingXL: CGFloat = 32
    static let spacing2XL: CGFloat = 48
    static let spacing3XL: CGFloat = 64
}

// 使用示例
VStack(spacing: .spacingMD) {
    Text("标题")
    Text("内容")
}
.padding(.horizontal, .spacingLG)
```

### 4.4 组件内间距规范

#### 按钮
- 水平内边距：16pt
- 垂直内边距：12pt
- 图标与文字间距：8pt

#### 卡片
- 内容内边距：16pt 或 20pt
- 卡片间间距：16pt

#### 列表项
- 垂直内边距：12pt 或 16pt
- 水平内边距：16pt
- 分隔线左侧缩进：52pt（图标 + 间距）

---

## 圆角系统

### 5.1 标准圆角值

| 名称 | 值 | 用途 |
|------|-----|------|
| None | 0pt | 无需圆角 |
| xs | 4pt | 小标签、徽章 |
| sm | 8pt | 小型按钮、输入框 |
| md | 12pt | 卡片、对话框 |
| lg | 16pt | 大卡片、模态框 |
| xl | 20pt | 大型容器 |
| 2xl | 24pt | 输入框（液态玻璃） |
| full | 9999pt | 圆形、完全圆角 |

### 5.2 Swift 圆角定义

```swift
extension CGFloat {
    static let cornerRadiusNone: CGFloat = 0
    static let cornerRadiusXS: CGFloat = 4
    static let cornerRadiusSM: CGFloat = 8
    static let cornerRadiusMD: CGFloat = 12
    static let cornerRadiusLG: CGFloat = 16
    static let cornerRadiusXL: CGFloat = 20
    static let cornerRadius2XL: CGFloat = 24
    static let cornerRadiusFull: CGFloat = 9999
}

// 预设圆角样式
struct CornerRadius {
    static let button: CGFloat = 8
    static let card: CGFloat = 16
    static let inputField: CGFloat = 22
    static let tabBar: CGFloat = 45
    static let avatar: CGFloat = 9999  // 圆形
}
```

### 5.3 组件圆角规范

| 组件 | 圆角值 | 说明 |
|------|--------|------|
| 按钮 | 8pt | 标准按钮 |
| 魔法按钮 | 50% (圆形) | 突出视觉 |
| 输入框 | 22pt | 液态玻璃风格 |
| Tab Bar | 45pt | 悬浮胶囊状 |
| 卡片 | 16pt | 内容卡片 |
| 头像 | 50% (圆形) | 用户头像 |
| 作品图 | 16pt | 作品缩略图 |
| 统计卡片 | 16pt | 数据统计卡片 |

---

## 阴影系统

### 6.1 标准阴影层级

| 层级 | 参数 | 用途 |
|------|------|------|
| None | - | 无阴影 |
| xs | color: #00000014, radius: 2, x: 0, y: 1 | 轻微悬浮 |
| sm | color: #0000001A, radius: 4, x: 0, y: 2 | 小型卡片 |
| md | color: #0000001F, radius: 8, x: 0, y: 4 | 标准卡片 |
| lg | color: #00000026, radius: 12, x: 0, y: 6 | 大卡片 |
| xl | color: #00000033, radius: 16, x: 0, y: 8 | 模态框 |
| 2xl | color: #0000004D, radius: 24, x: 0, y: 12 | 弹出菜单 |

### 6.2 Swift 阴影定义

```swift
extension View {
    // 标准阴影修饰符
    func shadowXS() -> some View {
        self.shadow(color: .black.opacity(0.08), radius: 2, x: 0, y: 1)
    }

    func shadowSM() -> some View {
        self.shadow(color: .black.opacity(0.1), radius: 4, x: 0, y: 2)
    }

    func shadowMD() -> some View {
        self.shadow(color: .black.opacity(0.12), radius: 8, x: 0, y: 4)
    }

    func shadowLG() -> some View {
        self.shadow(color: .black.opacity(0.15), radius: 12, x: 0, y: 6)
    }

    func shadowXL() -> some View {
        self.shadow(color: .black.opacity(0.2), radius: 16, x: 0, y: 8)
    }
}
```

### 6.3 组件阴影规范

| 组件 | 阴影配置 |
|------|---------|
| Tab Bar | `.shadow(color: .black.opacity(0.08), radius: 12, x: 0, y: 4)` |
| 统计卡片 | `.shadow(color: .black.opacity(0.08), radius: 8, x: 0, y: 4)` |
| 作品卡片 | `.shadow(color: .black.opacity(0.1), radius: 8, x: 0, y: 3)` |
| 菜单卡片 | `.shadow(color: .black.opacity(0.08), radius: 8, x: 0, y: 4)` |
| 生成结果 | `.shadow(color: .black.opacity(0.1), radius: 12, x: 0, y: 6)` |
| 魔法按钮 | `.shadow(color: accentBlue.opacity(0.45), radius: 12, x: 0, y: 4)` |

---

## Liquid Glass 效果

### 7.1 iOS 26 Liquid Glass 概述

iOS 26 引入了原生的 `.glassEffect()` 修饰符，用于创建液态玻璃视觉效果。

### 7.2 自动适配组件

以下系统组件**自动**应用 Liquid Glass：
- `NavigationStack`
- `NavigationSplitView`
- `UINavigationBar` (UIKit)

### 7.3 手动应用 Liquid Glass

```swift
// 基础用法
myView
    .glassEffect()
    .cornerRadius(16)

// 完整液态玻璃卡片
myView
    .glassEffect()
    .cornerRadius(16)
    .overlay(
        RoundedRectangle(cornerRadius: 16)
            .stroke(Color.white.opacity(0.3), lineWidth: 1)
    )
    .shadow(color: .black.opacity(0.08), radius: 8, x: 0, y: 4)
```

### 7.4 项目中的 Liquid Glass 变体

```swift
extension View {
    /// 标准液态玻璃 - Tab Bar
    func liquidGlass(cornerRadius: CGFloat = 45) -> some View {
        self
            .glassEffect()
            .cornerRadius(cornerRadius)
            .overlay(
                RoundedRectangle(cornerRadius: cornerRadius)
                    .stroke(Color.white.opacity(0.2), lineWidth: 1)
            )
            .shadow(color: .black.opacity(0.08), radius: 8, x: 0, y: 4)
    }

    /// 轻量液态玻璃 - 输入框
    func liquidGlassThin(cornerRadius: CGFloat = 22) -> some View {
        self
            .glassEffect()
            .cornerRadius(cornerRadius)
            .overlay(
                RoundedRectangle(cornerRadius: cornerRadius)
                    .stroke(Color.white.opacity(0.15), lineWidth: 0.5)
            )
    }

    /// 增强液态玻璃 - 卡片
    func liquidGlassProminent(cornerRadius: CGFloat = 16) -> some View {
        self
            .glassEffect()
            .cornerRadius(cornerRadius)
            .overlay(
                RoundedRectangle(cornerRadius: cornerRadius)
                    .stroke(Color.white.opacity(0.25), lineWidth: 1)
            )
            .shadow(color: .black.opacity(0.1), radius: 12, x: 0, y: 6)
    }
}
```

### 7.5 Liquid Glass 使用场景

| 组件 | 变体 | 圆角 | 描边 | 阴影 |
|------|------|------|------|------|
| Tab Bar | Standard | 45pt | 1pt, 30% | 8%, r8, y4 |
| 输入框 | Thin | 22pt | 0.5pt, 15% | - |
| 统计卡片 | Prominent | 16pt | 1pt, 25% | 8%, r8, y4 |
| 菜单卡片 | Prominent | 16pt | 1pt, 25% | 8%, r8, y4 |
| 作品卡片 | - | 16pt | - | 10%, r8, y3 |

---

## 组件库

### 8.1 导航组件

#### Navigation Bar (导航栏)

```swift
// iOS 26 自动适配 Liquid Glass
NavigationStack {
    Content()
        .navigationTitle("锦绣 AI")
        .navigationBarTitleDisplayMode(.inline)
        .toolbar {
            ToolbarItem(placement: .navigationBarLeading) {
                // 左侧按钮
            }
            ToolbarItem(placement: .navigationBarTrailing) {
                // 右侧按钮
            }
        }
}
```

**规格**:
- 高度：44pt (内容) + Safe Area
- 标题字体：17pt Semibold
- 按钮尺寸：30x30pt

### 8.2 输入组件

#### 文本输入框

```swift
HStack {
    TextField("placeholder", text: $text)
        .font(.suxiuBody)

    if !text.isEmpty {
        Button(action: send) {
            Image(systemName: "arrow.up.circle.fill")
                .font(.system(size: 32))
                .foregroundColor(.suxiuAccent)
        }
    }
}
.padding(.horizontal, 14)
.padding(.vertical, 10)
.glassEffect()
.cornerRadius(22)
```

**规格**:
- 高度：44pt
- 圆角：22pt
- 背景：Liquid Glass (Thin)
- 占位符颜色：Text Tertiary

### 8.3 按钮组件

#### 标准按钮

```swift
Button(action: {}) {
    Text("按钮文字")
        .font(.suxiuHeadline)
        .foregroundColor(.white)
        .frame(maxWidth: .infinity)
        .frame(height: 44)
        .background(Color.suxiuAccent)
        .cornerRadius(8)
}
```

#### 魔法按钮 (突出式)

```swift
Button(action: {}) {
    ZStack {
        Circle()
            .fill(LinearGradient(
                colors: [.suxiuAccent, .suxiuAccentDark],
                startPoint: .topLeading,
                endPoint: .bottomTrailing
            ))
            .frame(width: 50, height: 50)
            .shadow(color: .suxiuAccent.opacity(0.45), radius: 12, x: 0, y: 4)
            .overlay(
                Circle()
                    .stroke(Color.white.opacity(0.3), lineWidth: 1)
            )

        Image(systemName: "wand.and.stars")
            .font(.system(size: 20, weight: .semibold))
            .foregroundColor(.white)
    }
}
```

**规格**:
- 尺寸：50x50pt
- 形状：圆形
- 渐变：Accent Blue → Accent Blue Dark
- 阴影：45% Accent, r12, y4

### 8.4 卡片组件

#### 统计卡片

```swift
HStack {
    statItem(count: "24", label: "我的制作")
    Divider()
    statItem(count: "12", label: "赞")
    Divider()
    statItem(count: "6", label: "关注者")
}
.padding(.horizontal, 32)
.padding(.vertical, 16)
.glassEffect()
.cornerRadius(16)
.overlay(
    RoundedRectangle(cornerRadius: 16)
        .stroke(Color.white.opacity(0.3), lineWidth: 1)
)
.shadow(color: .black.opacity(0.08), radius: 8, x: 0, y: 4)
```

**规格**:
- 圆角：16pt
- 背景：Liquid Glass
- 描边：30% White, 1pt
- 阴影：8% Black, r8, y4

#### 作品卡片

```swift
Image("Work1")
    .resizable()
    .scaledToFill()
    .frame(width: 130, height: 130)
    .clipShape(RoundedRectangle(cornerRadius: 16))
    .shadow(color: .black.opacity(0.1), radius: 8, x: 0, y: 3)
```

**规格**:
- 尺寸：130x130pt
- 圆角：16pt
- 阴影：10% Black, r8, y3

### 8.5 列表组件

#### 菜单列表项

```swift
HStack(spacing: 14) {
    // 图标背景
    RoundedRectangle(cornerRadius: 10)
        .fill(bgColor)
        .frame(width: 38, height: 38)

    // 图标
    Image(systemName: "bag")
        .font(.system(size: 16, weight: .medium))
        .foregroundColor(.black)

    // 标题
    Text("我的订单")
        .font(.suxiuBody)
        .foregroundColor(.black)

    Spacer()

    // 箭头
    Image(systemName: "chevron.right")
        .font(.system(size: 12, weight: .semibold))
        .foregroundColor(.black)
}
.padding(.horizontal, 16)
.padding(.vertical, 15)
```

**规格**:
- 高度：55pt
- 图标容器：38x38pt, 圆角 10pt
- 分隔线：10% Gray, 0.5pt
- 左侧缩进：52pt

### 8.6 标签页组件

#### Tab Bar Item

```swift
Button(action: { selectedTab = tag }) {
    VStack(spacing: 3) {
        Image(systemName: selectedTab == tag ? "\(icon).fill" : icon)
            .font(.system(size: 20))
            .foregroundColor(selectedTab == tag ? .suxiuAccent : .black.opacity(0.5))
            .scaleEffect(selectedTab == tag ? 1.08 : 1.0)

        Text(label)
            .font(.system(size: 10, weight: selectedTab == tag ? .semibold : .regular))
            .foregroundColor(selectedTab == tag ? .suxiuAccent : .black.opacity(0.5))
    }
    .frame(maxWidth: .infinity)
    .padding(.vertical, 2)
}
.animation(.spring(response: 0.25), value: selectedTab)
```

**规格**:
- 图标：20pt
- 文字：10pt
- 选中态：108% 缩放，强调色
- 未选中态：50% 黑色透明度

---

## 图标系统

### 9.1 SF Symbols 使用

项目使用 SF Symbols 作为图标系统。

#### 常用图标

| 用途 | 图标名称 | 尺寸 |
|------|---------|------|
| 首页 | `house` / `house.fill` | 20pt |
| 市场 | `bag` / `bag.fill` | 20pt |
| 推荐 | `doc.text` / `doc.text.fill` | 20pt |
| 我的 | `person` / `person.fill` | 20pt |
| 魔法 | `wand.and.stars` | 20pt |
| 生成中 | `hourglass` | 20pt |
| 发送 | `arrow.up.circle.fill` | 32pt |
| 图片 | `photo` | 20pt |
| 麦克风 | `mic` | 20pt |
| 添加 | `plus` | 20pt |
| 设置 | `gearshape` | 20pt |
| 菜单 | `line.3.horizontal` | 20pt |
| 关闭 | `xmark.circle.fill` | 20pt |
| 返回 | `chevron.left` | 12pt |
| 收藏 | `heart` / `heart.fill` | 20pt |
| 订单 | `bag` | 20pt |
| 历史 | `clock` / `clock.fill` | 20pt |

### 9.2 图标使用规范

```swift
// 标准图标
Image(systemName: "house")
    .font(.system(size: 20))
    .foregroundColor(.primary)

// 选中态图标
Image(systemName: "house.fill")
    .font(.system(size: 20, weight: .medium))

// 工具栏图标
Image(systemName: "gearshape")
    .font(.system(size: 20))
    .foregroundColor(.primary)
```

---

## 动效设计

### 10.1 动画时长规范

| 动效类型 | 时长 | 曲线 | 用途 |
|---------|------|------|------|
| 微交互 | 150ms | Ease Out | 按钮 hover、图标切换 |
| 标准 | 250ms | Spring | Tab 切换、卡片展开 |
| 复杂 | 350ms | Ease In Out | 页面转场、模态框 |
| 加载中 | 1200ms | Linear (循环) | 魔法棒旋转 |

### 10.2 Swift 动画定义

```swift
extension Animation {
    // 微交互动画
    static let suxiuMicro = .easeOut(duration: 0.15)

    // 标准弹簧动画
    static let suxiuDefault = .spring(response: 0.25, dampingFraction: 0.8)

    // 页面转场动画
    static let suxiuTransition = .easeInOut(duration: 0.35)

    // 加载旋转动画
    static let suxiuSpin = .linear(duration: 1.2).repeatForever(autoreverses: false)
}
```

### 10.3 组件动效规范

#### Tab 切换

```swift
.animation(.spring(response: 0.25), value: selectedTab)

// 图标缩放
.scaleEffect(selectedTab == tag ? 1.08 : 1.0)
```

#### 魔法按钮旋转

```swift
.rotationEffect(.degrees(isGenerating ? 360 : 0))
.animation(
    isGenerating ?
        .linear(duration: 1.2).repeatForever(autoreverses: false) :
        .default,
    value: isGenerating
)
```

#### 结果出现

```swift
.transition(.opacity.combined(with: .scale(scale: 0.97)))
.withAnimation(.spring(response: 0.3))
```

#### 输入框按钮切换

```swift
.transition(.scale.combined(with: .opacity))
.animation(.spring(response: 0.3), value: promptText.isEmpty)
```

### 10.4 页面转场

```swift
// 导航推入
NavigationStack {
    content
        .animation(.suxiuTransition, value: navigationPath)
}

// 模态框呈现
.sheet(isPresented: $isPresented) {
    content
        .transition(.move(edge: .bottom).combined(with: .opacity))
}
```

---

## 附录

### A. 设计资源

- **Sketch 组件库**: [内部链接]
- **Figma 设计文件**: [内部链接]
- **SF Symbols 下载**: https://developer.apple.com/sf-symbols/

### B. 代码片段

完整的设计系统 Swift 包：

```swift
// SuxiuDesignSystem.swift

import SwiftUI

// MARK: - Colors
extension Color {
    static let suxiuBackground = Color(red: 0.945, green: 0.957, blue: 0.976)
    static let suxiuAccent = Color(red: 0.2, green: 0.48, blue: 0.95)
    static let suxiuTextPrimary = Color(red: 0.067, green: 0.094, blue: 0.153)
    // ... 更多颜色
}

// MARK: - Fonts
extension Font {
    static let suxiuTitle1 = Font.system(size: 28, weight: .bold, design: .default)
    static let suxiuBody = Font.system(size: 17, weight: .regular, design: .default)
    // ... 更多字体
}

// MARK: - Spacing
extension CGFloat {
    static let spacingXS: CGFloat = 4
    static let spacingSM: CGFloat = 8
    static let spacingMD: CGFloat = 16
    // ... 更多间距
}

// MARK: - Shadows
extension View {
    func shadowSM() -> some View { ... }
    func shadowMD() -> some View { ... }
    func shadowLG() -> some View { ... }
}

// MARK: - Liquid Glass
extension View {
    func liquidGlass(cornerRadius: CGFloat = 45) -> some View { ... }
    func liquidGlassThin(cornerRadius: CGFloat = 22) -> some View { ... }
    func liquidGlassProminent(cornerRadius: CGFloat = 16) -> some View { ... }
}
```

---

**文档结束**

*苏绣 AI 设计系统 v1.0.0*
